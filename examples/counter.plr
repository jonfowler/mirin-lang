fn counter
  { #clk: Clock, rstn: Reset @clk = high }
  ( const bits: usize )
  -> uint[bits] @clk
  {
    var count: uint[bits] @clk;
    count = (count + 1).reg{rstn, reset_val = 0}();
    return count;
  }

port DF
  { #clk: Clock }
  ( A: Type )
  = df {
    in ready: bool @clk,
    out valid: bool @clk,
    out data: A @clk,
  }

fn connectDf
  { #clk: Clock }
  ( upstream: DF{clk}(uint(8)), out downstream: DF{clk}(uint(8)) )
  {
    downstream = upstream;
  }

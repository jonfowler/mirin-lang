// A `bits` slice with two literal endpoints, low-first/ascending
// (planning/slicing.md). `x[4..8]` is the half-open interval [4, 8) — bits
// {4,5,6,7}, width 4 — and lowers to the SystemVerilog indexed part-select
// `x[4 +: 4]`.
fn s(x: bits(16)) -> bits(4) {
    x[4..8]
}

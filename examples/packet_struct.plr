struct Packet = packet {
  valid: bool,
  payload: uint[8],
}

fn registerPacket
  { #clk: Clock, rstn: Reset @clk = high }
  ( input: Packet @clk )
  -> Packet @clk
  {
    let output = input.reg{
      rstn,
      reset_val = packet {
        valid: false,
        payload: 0,
      },
    }();

    return output;
  }

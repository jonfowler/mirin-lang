fn shiftRegister
  { #clk: Clock, rstn: Reset @clk = high }
  ( input: uint(8) @clk )
  -> uint(8) @clk
  {
    let stage0 = input.reg(rstn, 0);
    let stage1 = stage0.reg(rstn, 0);
    return stage1;
  }
